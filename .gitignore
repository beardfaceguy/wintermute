# --- Vite / Frontend ---
.vitepress
.vite

# Build output
frontend/dist
frontend/.vite
frontend/node_modules/

# Playwright e2e artifacts
talkingHead/frontend/test-results/
talkingHead/frontend/playwright-report/
talkingHead/frontend/blob-report/
talkingHead/frontend/playwright/.cache/

# Local env vars or secrets (if any)
.env
.env.*
*/.env
*/.env.*
frontend/.env
frontend/.env.*

# --- Python / Backend ---
# Byte-compiled / cache files
__pycache__/
*.py[cod]
*.so

# Virtual environment
.venv/
venv/
env/
env.bak/
*.pyenv

# Python env/var files
backend/.env
backend/.env.*

# Pyright or VS Code Python configs
pyrightconfig.json
backend/.mypy_cache/
backend/.pytest_cache/

# Uvicorn or gunicorn logs (if used)
*.log

# Root env files
.env
.env.local

# Local SQLite databases (chat history, etc.)
wintermute_chat.db
*.sqlite
*.sqlite3

# --- General ---
# Node modules (global)
node_modules/

# System files
Thumbs.db
ehthumbs.db
*.swp
*.swo
.DS_Store

# Optional: ignore shell history
.history
.bash_history
.zsh_history


memory/chroma_store/

# TTS / STT voice model binaries — fetched out-of-band, never committed
thVoice/piper/models/*.onnx
thVoice/piper/build/
thVoice/models/*.bin

build/model_training/LLM/data/
model_training/hf.env
# Training artifacts / checkpoints
*.pt
*.npz
model_training/titanProject/logs/
titans-pytorch/

# Cursor temp artifacts
.cursor/tmp/
*.pem

# Local workstation-only Titan / AWS scratch state
.venv/
.cursor/mcp.json
.cursor/docs/online_docs/
Untitled
hf_token
params_*.json
params_*.py.json
aws-doc-sdk-examples/
mcp_aws/
openclaw/

# Runtime-generated LlamaIndex storage (rebuilt from source on startup)
storage/

# Training material datasets (large PR JSON files — kept locally only)
training_material/

# Model training outputs (binary weights, checkpoints, results)
model_training/titanProject/checkpoints_sft/
model_training/titanProject/results/

# Cursor AI IDE files (memory + rules are IDE-specific, not source)
.cursor/memory/
.cursor/rules/
# Note: .cursor/docs/ intentionally tracked (contains implementation docs)

# Generated agent run artifacts — rebuild on every run
agents/test_cases/last_freud_audit.json
agents/test_cases/last_run.json
