name: Daily Freud Audit
on:
  schedule:
    - cron: "0 3 * * *"   # 03:00 UTC daily
  workflow_dispatch:
jobs:
  freud:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run audit script
        run: |
          pip install -r requirements.txt
          python scripts/freud_audit.py  # TODO: implement
